name: Star History
on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:
permissions:
  contents: write
jobs:
  star-history:
    # Forks do not inherit STAR_HISTORY_TOKEN; run this workflow only upstream.
    if: ${{ github.repository == 'sub-store-org/Sub-Store' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: carsteneu/mystarhistory@v1
        with:
          repos: ${{ github.repository }}
          token: ${{ secrets.STAR_HISTORY_TOKEN }}
